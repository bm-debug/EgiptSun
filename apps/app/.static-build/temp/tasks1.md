## Задачи по скринкасту (админ-таблицы / роли / фильтры)

☑ 1) **Сортировка по умолчанию: всегда `title` (ASC)**
   - Ожидание: при открытии коллекции таблица сразу отсортирована по названию (и не "сбрасывается" в странное состояние).
   - Также проверить, что **ASC/DESC не перепутаны** в UI/логике.
   - **Проблемный файл**: `apps/site/src/components/application-blocks/data-table.tsx`

☑ 2) **Разделить видимость колонок по устройствам (desktop / tablet / mobile)**
   - Сейчас настройки видимости колонок сохраняются одним ключом на коллекцию и "перетирают" друг друга между режимами.
   - Нужно: независимые наборы колонок для каждого брейкпоинта + корректное восстановление при смене размера.
   - **Проблемные файлы**:
     - `apps/site/src/components/application-blocks/data-table.tsx`
     - `apps/site/src/shared/utils/table-settings.ts`
     - `apps/site/src/app/api/altrp/v1/admin/table-settings/route.ts`

☑ 3) **Разделить ширины колонок по устройствам (desktop / tablet / mobile)**
   - Сейчас есть только mobile vs desktop (`innerWidth < 1024`), а в требованиях звучит ещё tablet.
   - Нужно: 3 независимых профиля ширин + корректный выбор профиля и сохранение.
   - **Проблемный файл**: `apps/site/src/components/application-blocks/data-table.tsx`

☑  4) **Исправить "колонки повторяются / выключаю на мобилке — выключается на десктопе"**
   - Это следствие общей модели/ключей хранения настроек колонок.
   - Нужна внятная модель "view settings" (набор видимых колонок/порядок/ширина) отдельно для каждого вида.
   - **Проблемный файл**: `apps/site/src/components/application-blocks/data-table.tsx`

☑ 5) **Починить сохранение/восстановление локали `rs` для i18n JSON-полей**
   - В тексте звучит "пули RS не работает, не сохраняет нормально" + наблюдался "глюк" после сохранения.
   - Проверить конвертацию `entriesToLanguageObject` / `objectToEntries`, обработку ключей с суффиксами языков и fallback-логику.
   - **Проблемные файлы**:
     - `apps/site/src/components/application-blocks/data-table.tsx`
     - `apps/site/src/shared/utils/table-settings.ts`
     - `apps/site/src/app/api/altrp/v1/admin/table-settings/route.ts`

☑ 6) **Режим редактирования в таблице: убрать "глюки" и некорректные подстановки в поля**
   - Ожидание: при входе в edit-mode ничего "само" не подставляется/не ломается; отмена/сохранение работают предсказуемо.
   - **Проблемный файл**: `apps/site/src/components/application-blocks/data-table.tsx`

☑ 7) **Переделать UI фильтра по датам (date range) на более аккуратный компонент**
   - Сейчас dropdown с `DayPicker` выглядит "широко/стремно" и не соответствует ожиданиям (хочется уровень shadcn Calendar / более "идеальный" range picker).
   - Вариант: вынести в отдельный аккуратный popover/компонент и привести стили/верстку к текущему UI киту.
   - **Проблемный файл**: `apps/site/src/components/application-blocks/data-table.tsx`

☑ 8) **Роли: корректное отображение/редактирование `roles.title` (i18n) и связанных полей**
   - В тексте: ожидание, что "Администратор" отображается корректно (в т.ч. русскими), и что логика с суффиксами/локалями ведёт себя предсказуемо.
   - **Проблемный файл**: `apps/site/src/components/application-blocks/data-table.tsx`
